name: Publish to PyPi.org

on:
  release:
    types: [published]

jobs:
  publish_to_pypi:
    uses: hotosm/gh-workflows/.github/workflows/pypi_pdm_publish.yml@main
    with:
      pypi_token: ${{ secrets.PYPI_TOKEN }}
